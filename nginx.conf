server {
    listen       80;
    server_name  localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # 🌟 关键：支持 Vue Router 的 history 模式，防止刷新页面 404
        try_files $uri $uri/ /index.html;
    }

    # 如果你之前配了代理，也可以在 nginx 配，但目前我们直接用 IP 访问
}